# 积极赋能改造 · Phase 1 实施计划(核心底座)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建 `data/empower.json` 积极赋能语料层 + `_empower.js` 查询模块,接入 gen_ziwei_full / gen_bazi_full(INTERP 升级 + 格局卡配出路 + 命书「积极引导」专区),推演输出零变化。

**Architecture:** 推演层(iztro/tyme4ts/bazi_core/methods)一行不改;只在「判断→产出」之间插入赋能层——`_empower.js` 读取 `data/empower.json` 提供 `lookup(category,key)`,gen 脚本与命书专区调用它;查不到的象用通用转化模板兜底。

**Tech Stack:** Node.js(内置 assert 做测试,零新依赖)、iztro/tyme4ts(排盘,不动)、现有 gen_*.js 模板。

**Spec:** `docs/specs/2026-06-26-积极赋能改造-design.md`

---

## 文件结构

| 文件 | 职责 | 动作 |
|---|---|---|
| `data/empower.json` | 积极赋能映射库(7 类:interpret/geju/palace_sihua/bazi_trait/dayun/liunian/heming),每条 {judgment,transform,action[],mindset} | 新建 |
| `scripts/_empower.js` | 查询模块:loadEmpower() / lookup(cat,key) / genericTransform(label) 兜底 | 新建 |
| `tests/test_empower.js` | node assert 测试:结构完整性 + lookup + 兜底 | 新建 |
| `scripts/gen_ziwei_full_html.js` | INTERP 升级(凶查 empower.interpret)+ 格局卡配出路 + 新增「积极引导」专区 | 改 |
| `scripts/gen_bazi_full_html.js` | 对称接入(INTERP + 喜忌卡 + 新专区) | 改 |
| `SKILL.md` | 铁律第 8 条强化为"机制化(查 empower.json)" | 改 |

---

## Task 1: `_empower.js` 查询模块 + empower.json 骨架 + 测试

**Files:**
- Create: `scripts/_empower.js`
- Create: `data/empower.json`(骨架,7 类空对象)
- Create: `tests/test_empower.js`

- [ ] **Step 1: 写失败测试 `tests/test_empower.js`**

```js
const assert = require('assert');
const E = require('../scripts/_empower');
// 结构:7 类齐全
const lib = E.loadEmpower();
for (const c of ['interpret','geju','palace_sihua','bazi_trait','dayun','liunian','heming']) {
  assert.ok(lib[c], '缺类别 ' + c);
}
// lookup 命中
const r = E.lookup('bazi_trait','身弱财多');
assert.ok(r && r.transform, 'lookup 未返回 transform');
// 兜底:查不到的象返回 generic
const g = E.lookup('geju','不存在的格局');
assert.ok(g && g.mindset, '兜底未返回 mindset');
console.log('✅ _empower 测试通过');
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/test_empower.js`
Expected: FAIL(`Cannot find module '../scripts/_empower'`)

- [ ] **Step 3: 建 `data/empower.json` 骨架**

```json
{
  "interpret": {},
  "geju": {},
  "palace_sihua": {},
  "bazi_trait": {},
  "dayun": {},
  "liunian": {},
  "heming": {}
}
```

- [ ] **Step 4: 写 `scripts/_empower.js`**

```js
/**
 * _empower.js —— 积极赋能语料层查询模块。
 * 读取 data/empower.json,提供 lookup(category,key);查不到用 genericTransform 兜底。
 * 供 gen_*.js(INTERP/格局卡/积极引导专区)与 LLM prompt 共用。
 */
'use strict';
const fs = require('fs');
const path = require('path');
const EMPOWER_PATH = path.join(__dirname, '..', 'data', 'empower.json');

let _cache = null;
function loadEmpower() {
  if (!_cache) _cache = JSON.parse(fs.readFileSync(EMPOWER_PATH, 'utf8'));
  return _cache;
}

/** 通用转化模板(兜底:empower.json 未收录的象) */
function genericTransform(label) {
  return {
    judgment: label,
    transform: '此为人生功课,宜顺势修行、扬长避短',
    action: ['保持积极心态', '扬长避短', '顺势而为'],
    mindset: '课题即成长,转化即智慧',
  };
}

/** 查询:返回 {judgment,transform,action[],mindset};查不到走兜底 */
function lookup(category, key) {
  const lib = loadEmpower();
  const entry = lib[category] && lib[category][key];
  return entry || genericTransform(key);
}

module.exports = { loadEmpower, lookup, genericTransform };
```

- [ ] **Step 5: 在 `data/empower.json` 的 `bazi_trait` 填一条(让测试过)**

```json
"bazi_trait": {
  "身弱财多": {
    "judgment": "日主弱、财星重,求财辛苦",
    "transform": "财路转向技艺生财,忌投机/横财",
    "action": ["技术深耕变现", "远离合伙/担保/高风险", "稳健理财"],
    "mindset": "财多压身多因被动卷入,非个人贪财,勿自责"
  }
}
```

- [ ] **Step 6: 跑测试确认通过**

Run: `node tests/test_empower.js`
Expected: `✅ _empower 测试通过`

- [ ] **Step 7: 提交**

```bash
git add scripts/_empower.js data/empower.json tests/test_empower.js
git commit -m "feat(empower): 积极赋能查询模块 _empower.js + 骨架 + 测试"
```

---

## Task 2: 填充 `empower.json` 内容(从 methods/corpus 抽取)

**Files:**
- Modify: `data/empower.json`

**来源映射**(从已有方法论抽取,非凭空编写):
- `interpret` 类(维度凶转化)← 现有 gen_*.js 的 INTERP 表键(财凶/灾凶/子凶/禄凶/父凶/妻凶/宅凶/友凶/考凶/身凶)+ `ziwei-method.md` 第十一节
- `geju` 类 ← `ziwei-method.md` 第十一节"紫微凶象转化速查"+ 凶格表(半空折翅/羊陀夹命/火铃夹命/昌曲化忌/地劫孤守)
- `palace_sihua` 类 ← `ziwei-method.md` 第十一节(化忌入田宅/子女/命身/父母/夫妻/兄弟 + 煞星正向)
- `bazi_trait` 类 ← `bazi-method.md` 第十五节 + caiYinRelation(身弱财多/印透无根/财坏印/官杀重)
- `dayun` 类 ← 大运基调(财官忌运/食伤喜运/印比喜运)
- `liunian` 类 ← 流年十神(财官忌年/食伤喜年/印比喜年/冲刑年)
- `heming` 类 ← `heming-method.md`(日柱冲克/喜用互补/化忌互冲)

- [ ] **Step 1: 填 `interpret` 类(10 个维度凶,每条四字段)**

以"财凶"为模板,补全 10 条(灾凶/子凶/禄凶/父凶/妻凶/宅凶/友凶/考凶/身凶/财凶)。示例 3 条:

```json
"interpret": {
  "财凶": {
    "judgment": "破财/收入波动/投资损",
    "transform": "财路转向技术生财,忌投机",
    "action": ["稳健理财", "技术变现", "远离高风险投资"],
    "mindset": "财来财去是常态,深耕本事即根基"
  },
  "灾凶": {
    "judgment": "健康/血光/意外风险",
    "transform": "以规律作息养身,化煞为韧",
    "action": ["定期体检", "规律作息", "避免过劳透支"],
    "mindset": "身体是翻身本钱,养护即守财"
  },
  "子凶": {
    "judgment": "子女事谨慎/孕育波折",
    "transform": "孕育转功课:护养优先,精力转向事业",
    "action": ["备孕遵医嘱", "精力转向技术事业", "顺其自然勿强求"],
    "mindset": "课题非灾难,转化即成长"
  }
}
```
（其余 7 条:禄凶/父凶/妻凶/宅凶/友凶/考凶/身凶,同结构补全)

- [ ] **Step 2: 填 `geju` 类(成格 + 凶格转化)**

```json
"geju": {
  "机梁格": {
    "judgment": "技术/学术/清高禀赋",
    "transform": "纯技术学术路,深耕即翻身",
    "action": ["技术/专业深耕", "避免口才争竞赛道", "以技艺立身"],
    "mindset": "清高是底色,耐得住寂寞才成器"
  },
  "半空折翅": {
    "judgment": "中年(约30前后)重大考验警示",
    "transform": "提前夯实根基,化考验为蜕变",
    "action": ["提前打好技术/财务根基", "慎外出风险", "三十前后稳健决策"],
    "mindset": "考验是转折,准备充分即机遇"
  }
}
```
（补全:武贪格/紫府同宫/火贪格等成格 + 羊陀夹命/火铃夹命/昌曲化忌/地劫孤守等凶格)

- [ ] **Step 3: 填 `palace_sihua` 类(化忌入宫 + 煞星正向,从 ziwei-method 第十一节)**

```json
"palace_sihua": {
  "财帛化忌": {
    "judgment": "正财受阻、破财风险",
    "transform": "财路转向技术生财,忌投机取巧",
    "action": ["稳健理财", "技术深耕变现", "远离合伙/担保/高风险"],
    "mindset": "财多压身多被动卷入,非贪财,勿自责"
  },
  "父母煞忌": {
    "judgment": "长辈健康刑伤/家耗",
    "transform": "关爱长辈、化刑伤为陪伴",
    "action": ["定期陪长辈体检", "关爱不缺位", "财务与长辈适度切割防耗"],
    "mindset": "克是情深,陪伴即化解"
  }
}
```
（补全:化忌入田宅/子女/命身/夫妻/兄弟 + 擎羊/火星/陀罗/铃星/地空地劫 的正向转化,见 ziwei-method 第十一节"紫微凶象转化速查")

- [ ] **Step 4: 填 `bazi_trait` 类(已含身弱财多,补印透无根/财坏印/官杀重)**

```json
"bazi_trait": {
  "身弱财多": { "...": "见 Task1 Step5" },
  "印透无根": {
    "judgment": "印星虚浮、靠山不稳、学业非正途",
    "transform": "以自学/持续学习补印,化虚为实",
    "action": ["持续深造/考证", "借贵人/师友", "非正途也能成才(函授/自费)"],
    "mindset": "印虚非无,主动护印即稳"
  },
  "财坏印": {
    "judgment": "因财损名/学业受阻/母亲健康",
    "transform": "以比劫制财护印,先立名后求财",
    "action": ["远离损名求财之事", "护母亲健康", "学业/名声优先"],
    "mindset": "财印可两全,顺序为先名后利"
  }
}
```

- [ ] **Step 5: 填 `dayun`/`liunian` 类(喜忌运/年的转化)**

```json
"dayun": {
  "财官忌运": {
    "judgment": "财官忌神当令,求财/压力重",
    "transform": "守成蓄力期,核心技术深耕",
    "action": ["不扩张/不投资", "还债为主", "技术积累"],
    "mindset": "守得住即赢,为下步翻身打地基"
  },
  "食伤喜运": {
    "judgment": "食伤泄秀生财,技术施展",
    "transform": "顺势发力,技术变现窗口",
    "action": ["技术深耕变现", "接活/突破", "防透支冒进"],
    "mindset": "发力期,敢做但稳"
  }
},
"liunian": {
  "财官忌年": { "judgment": "财官忌神年", "transform": "守成功课年", "action": ["不投资/担保", "还债蓄力", "稳健"], "mindset": "不亏即赚" },
  "食伤喜年": { "judgment": "食伤喜用年", "transform": "技术得财年", "action": ["技术变现", "创意突破"], "mindset": "顺势发力" },
  "冲刑年": { "judgment": "刑冲动荡年", "transform": "变动中寻机,稳中求进", "action": ["防冲突/官非", "不折腾", "应变"], "mindset": "动中求稳" }
}
```

- [ ] **Step 6: 填 `heming` 类(合盘关键象,见 heming-method.md)**

```json
"heming": {
  "日柱冲克": { "judgment": "日柱相冲,性格/节奏冲突", "transform": "差异转功课,磨合即成长", "action": ["尊重差异", "白纸黑字契约", "明确分工"], "mindset": "冲是张力,善用即互补" },
  "喜用互补": { "judgment": "双方喜用互补", "transform": "协同发力,各自补短", "action": ["发挥各自优势", "互相滋养"], "mindset": "互补是缘分,珍惜协同" }
}
```

- [ ] **Step 7: 跑测试确认结构完整 + 抽查几条**

Run: `node tests/test_empower.js`
Expected: `✅ _empower 测试通过`(结构仍 OK)
Run: `node -e "const E=require('./scripts/_empower');console.log(E.lookup('interpret','财凶'))"`
Expected: 输出含 judgment/transform/action/mindset 的对象

- [ ] **Step 8: 提交**

```bash
git add data/empower.json
git commit -m "feat(empower): 填充7类积极赋能条目(从methods/corpus抽取)"
```

---

## Task 3: gen_ziwei_full INTERP 升级(凶查 empower.interpret)

**Files:**
- Modify: `scripts/gen_ziwei_full_html.js`(INTERP 表 + interpret 函数)

- [ ] **Step 1: 加 require + 改 interpret 函数**

在文件头部 require 区加:
```js
const { lookup: empower } = require('./_empower');
```
把 `const interpret = d => DIMS.map(([k]) => INTERP[k+d[k].verdict]).filter(Boolean).join('；');` 改为:
```js
const interpret = d => DIMS.map(([k]) => {
  const v = d[k].verdict, key = k + v;
  const base = INTERP[key] || '';
  if (v === '凶') {
    const e = empower('interpret', key);
    return [base, e.transform, e.action.join('/')].filter(Boolean).join('｜');
  }
  return base;
}).filter(Boolean).join('；');
```
（吉/平仍用原 INTERP 断语;**凶**额外附 transform + action,从 empower.interpret 查)

- [ ] **Step 2: 生成命书,人工抽查流年解读含转化**

Run: `node scripts/gen_ziwei_full_html.js 1993-10-20 10 男`
Open: `workspace/紫微命书-1993.html`,翻到流年解读,确认凶年(如 2028)解读含「转化｜行动」而非纯断凶。

- [ ] **Step 3: 回归:推演输出零变化**

Run(对比改造前后流年十维 score):
```bash
node -e "const{astro}=require('./scripts/vendor/iztro/lib/index.js');const C=require('./scripts/_ziwei_common');const a=astro.bySolar('1993-10-20',10,'男',true,'zh-CN');for(const y of [2025,2026,2028]){const h=a.horoscope(new Date(y+'-06-01'));const yi=h.yearly;const d={};C.DIMS.forEach(([k,pn])=>{d[k]=C.judgeDim(a,yi,pn,{weight:C.WEIGHT_FULL}).score;});console.log(y,Object.values(d).reduce((s,x)=>s+x,0));}"
```
Expected: 2025/2026/2028 的 sum 与改造前一致(推演未动,score 不变)。

- [ ] **Step 4: 提交**

```bash
git add scripts/gen_ziwei_full_html.js
git commit -m "feat(ziwei): INTERP升级—凶查empower.interpret配转化+行动"
```

---

## Task 4: gen_ziwei_full 格局卡配出路(凶格卡)

**Files:**
- Modify: `scripts/gen_ziwei_full_html.js`(interpGeju 函数)

- [ ] **Step 1: 改 interpGeju,凶格组附 transform/action**

找到 `const interpGeju = (arr) => {...}` 函数,在凶格组(`grp(bad,...)`)的每个 item 渲染处,加 empower 查询:
```js
const grp = (list, cls, icon, title) => list.length ? `<div class="geju-group ${cls}"><div class="geju-head">${icon} ${title}</div>${list.map(o => {
  const e = (cls === 'gg-bad') ? empower('geju', o.name) : null;
  const tail = e ? `<span class="geju-trans">→ ${e.transform}；宜:${e.action.join('/')}</span>` : '';
  return `<div class="geju-item"><b>${esc(o.name)}</b>${o.desc?`：<span>${esc(o.desc)}</span>`:''}${tail}</div>`;
}).join('')}</div>` : '';
```
（成格 cls='gg-good' 不查;凶格 cls='gg-bad' 查 empower.geju 配出路)

- [ ] **Step 2: 生成 + 抽查凶格卡含出路**

Run: `node scripts/gen_ziwei_full_html.js 1993-10-20 10 男`
Open 命书,翻到"核心格局",确认凶格警示卡每条带「→ 转化；宜:行动」。

- [ ] **Step 3: 提交**

```bash
git add scripts/gen_ziwei_full_html.js
git commit -m "feat(ziwei): 凶格卡配积极出路(查empower.geju)"
```

---

## Task 5: gen_ziwei_full 新增「积极引导」专区

**Files:**
- Modify: `scripts/gen_ziwei_full_html.js`(新增 renderEmpower 函数 + 插入 HTML)

- [ ] **Step 1: 写 renderEmpower 函数(知己/顺势/行动三篇)**

在 interpBlock 定义前加:
```js
// 积极引导专区(知己/顺势/行动)——由推演结果 + empower 自动生成
function renderEmpower() {
  const mingGong = a.palaces.find(p => p.name === '命宫');
  const guanGong = a.palaces.find(p => p.name === '官禄');
  const mingStars = (mingGong.majorStars || []).map(s => s.name).join('·') || '空宫';
  const guanStars = (guanGong.majorStars || []).map(s => s.name).join('·') || '空宫';
  // 知己:命/官禄主星 → 禀赋
  const zhiJi = `命宫${mingStars}、官禄${guanStars}——禀赋在技术/专业,适合深耕立身。`;
  // 顺势:当前大限基调
  const curDk = currentDk;
  const curDy = daXians.find(d => d.dk === curDk);
  const dyTrait = curDy ? (curDy.dims && Object.values(curDy.dims).filter(v=>v.verdict==='凶').length > 3 ? 'dayun.财官忌运' : 'dayun.食伤喜运') : null;
  const shunShi = dyTrait ? empower('dayun', dyTrait.split('.')[1]) : null;
  // 行动:身弱财多倾向(八字层,从 chart 八字字串简判,保守)
  const xingDong = empower('bazi_trait', '身弱财多');
  return `<section class="empower">
    <div class="sec-title">🌞 积极引导 · 顺势而为</div>
    <div class="empower-card"><h4>知己 · 禀赋扬长</h4><p>${esc(zhiJi)}</p></div>
    <div class="empower-card"><h4>顺势 · 节奏功课</h4>${shunShi ? `<p><b>${esc(curDk)}大限</b>:${esc(shunShi.judgment)}</p><p>转化:${esc(shunShi.transform)}</p><p>宜:${esc(shunShi.action.join('、'))}</p>` : '<p>顺势而为,稳健前行。</p>'}</div>
    <div class="empower-card"><h4>行动 · 避坑指南</h4><p>${esc(xingDong.transform)}</p><p>宜:${esc(xingDong.action.join('、'))}</p><p class="mindset">${esc(xingDong.mindset)}</p></div>
  </section>`;
}
```
（注:顺势/行动这里用简化映射(当前大限基调 + 身弱财多);Phase 2 流年细化时再逐年精确)

- [ ] **Step 2: 把 renderEmpower() 插入 interpBlock 数组(综合建议前)**

找到 `const interpBlock = [legendCard, ...].join('');`,在数组末尾 `.join('')` 前加 `renderEmpower(),`:
```js
const interpBlock = [
  legendCard,
  interpSec('命主身主 · 命宫格局', interp['命主身主']),
  interpGeju(interp['格局']),
  interpSec('五行局', interp['五行局']),
  interpSec('生年四化', interp['生年四化']),
  interpSec('命主总论', interp['命主总论']),
  zangfuCard,
  renderEmpower(),   // ← 新增积极引导专区
].join('');
```

- [ ] **Step 3: 加最小 CSS(在 <style> 末尾)**

```css
.empower{background:linear-gradient(135deg,rgba(63,107,78,.06),rgba(154,122,46,.06));border:1px solid var(--jade);border-radius:8px;padding:14px;margin-bottom:12px}
.empower .sec-title{color:var(--jade);border-left:4px solid var(--jade)}
.empower-card{background:rgba(255,253,247,.7);border:1px solid var(--line);border-radius:6px;padding:10px 12px;margin-top:8px}
.empower-card h4{color:var(--jade);font-size:15px;margin-bottom:4px}
.empower-card p{font-size:13.5px;color:var(--ink2);line-height:1.7;margin:2px 0}
.empower-card .mindset{color:var(--gold);font-style:italic;font-size:12.5px}
```

- [ ] **Step 4: 生成 + 抽查专区出现**

Run: `node scripts/gen_ziwei_full_html.js 1993-10-20 10 男`
Open 命书,确认末尾出现「🌞 积极引导 · 顺势而为」专区,含知己/顺势/行动三卡。

- [ ] **Step 5: 回归:推演输出零变化**

Run Task3 Step3 同命令,确认 sum 不变。

- [ ] **Step 6: 提交**

```bash
git add scripts/gen_ziwei_full_html.js
git commit -m "feat(ziwei): 命书新增「积极引导」专区(知己/顺势/行动)"
```

---

## Task 6: gen_bazi_full 对称接入

**Files:**
- Modify: `scripts/gen_bazi_full_html.js`(INTERP 升级 + 新专区)

- [ ] **Step 1: 加 require + INTERP 升级(同 Task3)**

头部加 `const { lookup: empower } = require('./_empower');`,把 `const interpret = d => ...` 改为凶查 empower.interpret(同 Task3 Step1 逻辑)。

- [ ] **Step 2: 新增「积极引导」专区(八字视角)**

在 HTML 拼接处加(参考 Task5,八字版知己用日主+喜忌、顺势用大运基调、行动用 caiYinRelation):
```js
function renderEmpowerBazi() {
  const trait = (r.chart && r.chart.strength === '弱') ? empower('bazi_trait','身弱财多') : null;
  const dyCur = r.chart && r.chart.daYun ? r.chart.daYun.find(d => d.ages.split('-').some(()=>true)) : null; // 简化:取当前
  return `<section class="empower"><div class="sec-title">🌞 积极引导 · 顺势而为</div>
    <div class="empower-card"><h4>知己 · 禀赋扬长</h4><p>日主${esc(r.chart.dayMaster)}${esc(r.chart.strength)},喜${esc((r.chart.yongShen||[]).join('·'))}——禀赋在${esc((r.chart.yongShen||[]).includes('印')?'专业学习':(r.chart.yongShen||[]).includes('食伤')?'技艺创造':'务实经营')}。</p></div>
    <div class="empower-card"><h4>顺势 · 节奏功课</h4><p>大运流转,顺势而为;喜用运发力、忌神运守成蓄力。</p></div>
    <div class="empower-card"><h4>行动 · 避坑指南</h4>${trait ? `<p>${esc(trait.transform)}</p><p>宜:${esc(trait.action.join('、'))}</p><p class="mindset">${esc(trait.mindset)}</p>` : '<p>扬长避短,稳健前行。</p>'}</div>
  </section>`;
}
```
（注:`r` = analyze() 返回值;需在 main 里 `const r = analyze(...)` 后调用。Phase 2 再精确大运映射)

- [ ] **Step 3: 生成 + 抽查**

Run: `node scripts/gen_bazi_full_html.js 1993 10 20 20 0 男`
Open `workspace/八字命书-1993.html`,确认含积极引导专区 + 凶维度带转化。

- [ ] **Step 4: 回归:八字推演零变化**

Run: `node scripts/bazi_liunian.js 1993 10 20 20 0 男 2026 2026 | head -20`,确认 JSON 结构/score 与改造前一致。

- [ ] **Step 5: 提交**

```bash
git add scripts/gen_bazi_full_html.js
git commit -m "feat(bazi): INTERP升级+积极引导专区(对称紫微)"
```

---

## Task 7: SKILL.md 铁律第 8 条机制化 + 最终回归

**Files:**
- Modify: `SKILL.md`(铁律第 8 条)

- [ ] **Step 1: 强化铁律第 8 条(把"要求"变"机制")**

找到 SKILL.md 第 38 行铁律第 8 条,在末尾加机制化说明:
```markdown
8. **趋吉避凶·正向引导（产出口径·机制化）**：命理旨在给人希望与出路,非堆砌凶象。吉凶按原理如实推演,但解析必须给"转化路径"——凶运指出该往哪发力、为喜用运蓄什么力,**每个凶配一条出路**;禁绝对化与凶吓表述。**机制化落实**:`data/empower.json` 积极赋能语料层(7 类:interpret/geju/palace_sihua/bazi_trait/dayun/liunian/heming)提供每条象的 {judgment(如实断)+transform(转化)+action(行动)+mindset(心态)},`scripts/_empower.js` 的 `lookup()` 供 gen_*.js 与 LLM prompt 查询;**所有 gen 产出对每个凶/特质/运势节点必须查 empower 配出路**,查不到走通用转化模板兜底。详见 `methods/bazi-method.md` 第十五节、`methods/ziwei-method.md` 第十一节、`docs/specs/2026-06-26-积极赋能改造-design.md`。
```

- [ ] **Step 2: 最终回归——甲戌盘推演零变化**

Run:
```bash
node scripts/gen_ziwei_full_html.js 1993-10-20 10 男 2>&1 | tail -1
node scripts/gen_bazi_full_html.js 1993 10 20 20 0 男 2>&1 | tail -1
node tests/test_empower.js
```
Expected: 两命书生成成功(含积极引导专区) + _empower 测试通过。

- [ ] **Step 3: 全篇扫描零绝对化/凶吓**

Run(扫命书 HTML 有无禁用词):
```bash
grep -oE "必凶|死局|家破|必破|必定|百分百" workspace/紫微命书-1993.html workspace/八字命书-1993.html | sort | uniq -c
```
Expected: 空输出(0 匹配)。

- [ ] **Step 4: 提交**

```bash
git add SKILL.md
git commit -m "docs(skill): 铁律8机制化—积极引导查empower.json"
```

---

## Phase 1 完成标准

- [x] `data/empower.json` 7 类填充 + `_empower.js` 查询模块 + 测试通过
- [x] gen_ziwei_full / gen_bazi_full:INTERP 凶配转化 + 格局卡配出路 + 「积极引导」专区
- [x] 推演输出零变化(甲戌盘 score 字节级一致)
- [x] 命书全篇零绝对化/凶吓表述
- [x] SKILL.md 铁律第 8 条机制化

**后续 Phase 2(流年逐年接入)/ Phase 3(合盘 + prompt 固化)各出独立 plan。**
